query PokemonList {
    pokemons(limit: 1154, offset: 0) {
        count
        next
        previous
        nextOffset
        prevOffset
        status
        results {
            id
            url
            name
            artwork
        }
    }
}